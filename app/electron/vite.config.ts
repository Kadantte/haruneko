import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
    //plugins: [],
    //publicDir: 'public',
    build: {
        sourcemap: true,
        outDir: 'build',
    }
});